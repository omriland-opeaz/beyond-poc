One-line: the Memory tab — what shapes the next answer, grouped under section headers.

```jsx
<MemoryRow head="You are measured on share, not volume." note="from six questions since June" when="12 Aug" onForget={…} />
```
