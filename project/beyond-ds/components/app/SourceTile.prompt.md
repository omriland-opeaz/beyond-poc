One-line: a source in the left column of Connect, with a dotted flow running from it to the lake.

```jsx
<SourceTile initials="TP" name="Tedis Pharma" foot="sell-in · 1 214 rows · 14 min ago" state="live" />
<SourceTile initials="SFT" name="SFTP · Bouaké" foot="no file since 9 Aug" state="broken"
  action={<Button variant="danger" size="sm">Reconnect</Button>} />
```
