One-line: use for the product name inside body copy so it never renders as ordinary text.

```jsx
<p>Connect whatever you have. <Wordmark /> reads it and never writes back.</p>
<p style={{ color: "var(--by-cream)" }}>… and <Wordmark color="var(--by-cream)" /> refreshes them.</p>
```
