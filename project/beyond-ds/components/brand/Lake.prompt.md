One-line: the lake — the destination every source flows into; use it on Connect, on empty states about the lake, and as the brand's hero drawing.

```jsx
<Lake rows="4.3M" foot="recomputed 14 min ago" scale={1.08} />
```

Grow `scale` as sources connect — the lake getting bigger is the feedback. Never put a shadow on it, never make the shoreline an ellipse.
