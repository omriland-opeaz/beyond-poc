One-line: where the user asks the lake a question in plain language.

```jsx
<AskInput value={draft} onChange={setDraft} onSubmit={ask} />
<AskInput size="sm" placeholder="Ask a follow-up" value={draft} onChange={setDraft} onSubmit={ask} />
```

Nothing on the home screen may compete with it.
